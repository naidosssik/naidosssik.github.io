export function initLevelsTool(options) {
  const {
    state,
    canvas,
    ctx,
    getOriginalImageData,
    setOriginalImageData,
    updateInfoPanel,
    updateChannelPreviews,
    applyChannels,
  } = options;

  console.log("Levels tool initialized");
}